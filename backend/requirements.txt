# =============================================================================
# SENTINEL Backend — Python Dependencies
# =============================================================================
# Pin major versions for reproducibility.
# Run: pip install -r requirements.txt

# ----- Web Framework -----
fastapi==0.109.2
uvicorn[standard]==0.30.1      # ASGI server with uvloop + httptools

# ----- Database -----
sqlalchemy[asyncio]==2.0.30    # Async ORM
asyncpg==0.29.0                # Async PostgreSQL driver
alembic==1.13.1                # Database migrations

# ----- Authentication -----
passlib==1.7.4                 # Password hashing
bcrypt==3.2.2                  # Pin bcrypt to 3.2.2 to fix passlib bug
PyJWT==2.8.0                   # JWT encoding/decoding

# ----- Validation & Serialization -----
pydantic==2.7.1                # Data validation (FastAPI uses this natively)
pydantic-settings==2.2.1       # Settings management from env vars
email-validator==2.1.1         # Email validation for Pydantic

# ----- HTTP -----
httpx==0.27.0                  # Async HTTP client (used in tests)
python-multipart==0.0.9        # Form data / file upload support

# ----- Rate Limiting -----
slowapi==0.1.9                 # Rate limiting for FastAPI (built on limits)

# ----- NLP -----
nltk==3.8.1
spacy==3.7.4
emoji==2.12.1                  # Emoji-to-text conversion

# ----- Machine Learning -----
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2
joblib==1.4.2                  # Model serialization

# ----- Explainability -----
lime==0.2.0.1

# ----- Utilities -----
python-dotenv==1.0.1           # .env file loading
structlog==24.1.0              # Structured logging
